# Emma's Website

### How to build

1. Get jekyll installed (follow [this](https://jekyllrb.com/docs/))
2. run `jekyll serve --live-reload` to run the dev server
3. run `jekyll build` to build the website into _site. Copy it out to wherever else to use it as a static site ;)
